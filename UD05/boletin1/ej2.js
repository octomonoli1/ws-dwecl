/* Escribe un pequeño programa con el que captures el evento click 
sobre un elemento y tengas acceso al objeto evento. Muestra la estructura 
del objeto en la consola usando console.log() y repasa todas las 
propiedades y métodos que ofrece para que tengas una comprensión completa
 de todo lo que contiene */
 
window.onload = (event) =>{
    console.log(event);
}

